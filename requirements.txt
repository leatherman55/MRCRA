# Install a CUDA-enabled PyTorch wheel first on NVIDIA systems; see README.md.
torch>=2.2
mlx>=0.31,<0.33; platform_system == "Darwin" and platform_machine == "arm64"

# FineWeb streaming, tokenization, telemetry, and dashboard support.
datasets>=4.0,<5
huggingface-hub>=1.0,<2
psutil>=5.9
trackio[cpu]>=0.31,<0.32
transformers>=5.0,<6
nvidia-ml-py>=12.0; platform_system == "Windows" or platform_system == "Linux"

# Verification suite.
coverage>=7.6
hypothesis>=6.100
mpmath>=1.3
pytest>=8.3

# Editable local package, including the bundled Trackio Spectral Network frontend.
-e .
